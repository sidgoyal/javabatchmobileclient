# javabatchmobileclient

Current Status : In Development

next steps : add automation testing 
           : build the rest client service
           : refactor everything (specifically the DS apis )
           : build UI
